<?xml version="1.0" encoding="utf-8"?>
<!-- SPDX-License-Identifier: GPL-3.0-or-later -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Foreground service for sync der ikke må afbrydes — adde'es kun
         hvis vi senere implementerer en aktiv sync-service. WorkManager med
         setExpedited() er ofte nok og kræver ingen permission. -->

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.DeltaSync"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".EnrollActivity"
            android:exported="false"
            android:label="Enroll device" />

        <activity
            android:name=".SetupActivity"
            android:exported="false"
            android:label="Set up database" />

        <activity
            android:name=".ShareActivity"
            android:exported="false"
            android:label="@string/share_title" />

    </application>

</manifest>
